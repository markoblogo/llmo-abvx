# ============================================
# Next.js Application
# ============================================
NODE_ENV=production
# Production environment (development, production)

NEXTAUTH_URL=http://localhost:3000
# NextAuth.js base URL - must be the full URL (e.g., https://llmo.abvx.xyz)
# For local development: http://localhost:3000
# For production: https://your-domain.com

NEXTAUTH_SECRET=your_nextauth_secret_here
# NextAuth.js secret key for encrypting sessions
# Generate with: openssl rand -base64 32

# ============================================
# Site Configuration
# ============================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Public site URL for absolute links, emails, and API callbacks
# For local development: http://localhost:3000
# For production: https://your-domain.com

NEXT_PUBLIC_BOOK_POPUP_ENABLED=false
# Enable/disable welcome modal with book offer (true/false)

NEXT_PUBLIC_AMAZON_BOOK_URL=https://amazon.com/dp/B0FYRSSZKL
# Amazon book URL for "LLMO: The Next SEO Revolution"

NEXT_PUBLIC_BOOK_DOWNLOAD_URL=/books/LLMO_Quick_Start.pdf
# URL path for free PDF book download (relative or absolute)

# ============================================
# Supabase (Database & Auth)
# ============================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
# Supabase project URL (public, used client-side)
# Get this from your Supabase project settings

NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Supabase anonymous/public key (used client-side)
# Get this from your Supabase project settings → API → Project API keys

SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
# Supabase service role key (server-side only, bypasses RLS)
# ⚠️ NEVER expose this in client-side code
# Get this from your Supabase project settings → API → Project API keys

# ============================================
# NextAuth.js Providers
# ============================================
GOOGLE_CLIENT_ID=your_google_client_id_here
# Google OAuth Client ID from Google Cloud Console
# Create OAuth 2.0 credentials at: https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# Google OAuth Client Secret from Google Cloud Console
# ⚠️ NEVER expose this in client-side code

# ============================================
# Resend (Email Service)
# ============================================
RESEND_API_KEY=re_your_resend_api_key_here
# Resend API key for sending transactional emails
# Get this from: https://resend.com/api-keys
# Format: re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

EMAIL_FROM=LLMO Directory <noreply@your-domain.com>
# Default sender email address for Resend
# Must be from a verified domain in Resend dashboard

CONTACT_EMAIL=support@your-domain.com
# Contact email address for contact form submissions
# Emails sent via contact form will be forwarded here

ADMIN_EMAIL=admin@your-domain.com
# Admin email address for notifications and fallback contact
# Used for admin notifications and error reports

# ============================================
# Stripe (Payments)
# ============================================
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# Stripe secret key for server-side API calls
# For development: use test key (sk_test_...)
# For production: use live key (sk_live_...)
# Get this from: https://dashboard.stripe.com/apikeys

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
# Stripe publishable key (used client-side, public)
# For development: use test key (pk_test_...)
# For production: use live key (pk_live_...)
# Get this from: https://dashboard.stripe.com/apikeys

NEXT_PUBLIC_STRIPE_PRICE_ID=price_your_stripe_price_id_here
# Stripe Price ID for Pro subscription plan
# Create a price in Stripe Dashboard → Products → Add Price
# Format: price_xxxxxxxxxxxxxxxxx

STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here
# Stripe webhook signing secret for verifying webhook events
# Get this from: Stripe Dashboard → Developers → Webhooks → Add endpoint
# After creating webhook, click on it and copy "Signing secret"
# Format: whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# OpenAI (AI Analysis)
# ============================================
OPENAI_API_KEY=sk-your_openai_api_key_here
# OpenAI API key for AI content analysis features
# Get this from: https://platform.openai.com/api-keys
# Format: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Cron Jobs & Scheduled Tasks
# ============================================
CRON_SECRET=your_cron_secret_here
# Secret token for authenticating cron job requests (e.g., Vercel Cron)
# Generate a random string (e.g., openssl rand -hex 32)
# Use this in your cron job URL: ?token=your_cron_secret_here

# ============================================
# Database (Prisma - optional, if using)
# ============================================
DATABASE_URL=postgresql://user:password@host:5432/database
# PostgreSQL database URL for Prisma (if not using Supabase only)
# Format: postgresql://[user[:password]@][host][:port][/database]
# Usually same as Supabase connection string

# Note:
# Copy this file to `.env.local` and fill in real credentials for local development.
# On Vercel, set the same variables in the project settings (Environment Variables).
